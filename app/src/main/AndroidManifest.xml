<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Google Account permissions for Gmail API -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <!-- Notification listener permission -->
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
        tools:ignore="ProtectedPermissions" />

    <!-- Optional: For background processing -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Location for Maps/Geofencing -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Background location for geofence triggers when app is not in use (Android 10+) -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Optional: For storing logs/data -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- For reading images on Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LocalLLMApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- LoginActivity as launcher -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Model Selection Activity -->
        <activity
            android:name=".ModelSelectionActivity"
            android:exported="false"
            android:theme="@style/Theme.LocalLLMApp" />

        <!-- Instruction Model Activity -->
        <activity
            android:name=".InstructionModelActivity"
            android:exported="false"
            android:theme="@style/Theme.LocalLLMApp" />

        <!-- Multi Modal Activity -->
        <activity
            android:name=".MultiModalActivity"
            android:exported="false"
            android:theme="@style/Theme.LocalLLMApp" />

        <!-- Workflow Setup Activity -->
        <activity
            android:name=".WorkflowSetupActivity"
            android:exported="false"
            android:theme="@style/Theme.LocalLLMApp" />

        <!-- Person Enrollment Activity for Photos workflows -->
        <activity
            android:name=".photos.PersonEnrollmentActivity"
            android:exported="false"
            android:theme="@style/Theme.LocalLLMApp" />

        <!-- Map Picker Activity -->
        <activity
            android:name=".maps.MapPickerActivity"
            android:exported="false"
            android:theme="@style/Theme.LocalLLMApp" />

        <!-- Notification listener service -->
        <service
            android:name=".MyNotificationListenerService"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
            <meta-data
                android:name="android.service.notification.listener.component"
                android:value="com.example.localllmapp/com.example.localllmapp.MyNotificationListenerService" />
        </service>

        <!-- Broadcast receiver for processed notification data -->


        <!-- Google Play Services metadata -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- Maps API Key (set value in res/values/strings.xml) -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <!-- Geofence receiver -->
        <receiver
            android:name=".geofence.GeofenceReceiver"
            android:exported="false" />

        <!-- Restore geofences after device reboot -->
        <receiver
            android:name=".geofence.BootCompletedReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>