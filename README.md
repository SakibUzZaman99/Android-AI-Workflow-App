# Android AI Workflow App
an app that uses on device locally run LLM on Android phones that manage workflows set by users (receiving messages/gps triggers / photos triggers) that causes the workflow to kickstart and send messages/media out
